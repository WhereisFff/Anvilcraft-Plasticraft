repositories {
    exclusiveContent {
        forRepositories(
            mavenCentral(),
            maven {
                name = 'Cjsah Maven'
                url = 'https://server.cjsah.net:1002/maven/'
                content {
                    includeGroup 'dev.anvilcraft.lib'
                    includeGroup 'dev.dubhe'
                    includeGroup 'dev.anvilcraft.resource'
                }
            }
        )
        filter {
            includeGroup 'dev.anvilcraft.lib'
            includeGroup 'dev.dubhe'
            includeGroup 'dev.anvilcraft.resource'
        }
    }
    maven {
        name = 'Modrinth Maven'
        url = 'https://api.modrinth.com/maven'
        content { includeGroup 'maven.modrinth' }
    }
    maven {
        name = "Jared's Maven"
        url = 'https://maven.blamejared.com/'
        content { includeGroup 'mezz.jei' }
    }

    // 本地 Maven 仅作为其他开发产物的备用来源；AnvilCraft 始终使用远程仓库。
    mavenLocal {
        content {
            excludeGroupByRegex 'dev\\.dubhe(\\..*)?'
            excludeGroupByRegex 'dev\\.anvilcraft(\\..*)?'
        }
    }
}
